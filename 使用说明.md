# 鲸鱼娘桌面陪伴

v1.4.5：统一修复全部勾选菜单的点击与显示。开机自启动读取实际登记状态，设置后核验并保存；电脑状态、原地不动、自动探索、流畅动画和静默更新均使用同一套状态绑定。菜单重新打开时刷新，皮肤和透明度保持单选；深色菜单使用清晰的勾选图标。电脑状态仍在每次启动时默认显示。

制作者：lnc

v1.4.4 修订已修复「原地不动」点击后只勾选、不生效的问题，开启和关闭都会保存。GPU 占用优先显示 Windows 最忙引擎的占用率；请对照任务管理器中同一块显卡的总占用率，而非某个指定的 3D、复制或视频图表。面板底部标明 Windows 引擎或驱动备用，监测详情可查看显卡名称和占用来源。两边采样时刻不同可能造成瞬时数值差异。

v1.4.4 文件保护：安装及更新后会自动在 `%LOCALAPPDATA%\Programs\protecting` 部署独立保护程序并登记当前用户登录启动。登录后等待约 45 秒，检查安装包文件的 SHA-256；完整但未启动时先尝试启动一次，缺失、损坏或启动失败时从正式 GitHub Release 下载已校验的最新版并静默修复，完成后退出。保留原安装位置、个人设置和额外皮肤；失败回滚，一次登录不反复重试。普通 git push 不触发修复版本发布，不降级到旧 Release。

关闭保护：打开上述 protecting 文件夹，双击 **prohibit.vbs**（Windows 脚本文件，不是普通 TXT）。它会静默移除保护启动项并创建 disabled 标记；后续更新和重装尊重此选择。桌宠自身的「开机自启动」开关与保护程序独立，关闭桌宠自启动后，保护只修复损坏文件，不主动开启桌宠。卸载桌宠前先关闭保护，避免下次登录重新安装。日志保存在 protecting/protection.log。

保护程序全程不显示检查、下载或失败窗口，不请求管理员权限；修复成功后桌宠和电脑状态界面按正常方式恢复。保护启动的桌宠不会自动弹出温度权限或更新对话框，CPU 测温按钮仍可手动使用。首次安装和实际更新都包含此保护部署步骤。



右键桌宠 →“关于与素材来源”，可查看制作者名称与素材署名。

运行 BlueWhalePet.exe，保持整个文件夹完整。无聊天、无需账号；启动后后台检查正式版本更新。

## 换皮肤

右键桌宠或任务栏托盘图标 →「换皮肤」，可选择「鲸鱼娘 · 经典女仆」「诺艾尔 · 原神」「新约能天使 · 明日方舟」。选中立即生效，菜单勾选当前皮肤；下次启动自动恢复。位置、大小、监测透明度、正在执行的动作与自主行为计划继续保留。攀爬和悬挂会按各皮肤的手部位置对齐边框。

## 交互与原地不动（v1.4.4）

摸摸头、喂一碗白饭、一起玩、走一走、睡觉/叫醒，统一放在右键「交互动作」子菜单。

勾选右键「原地不动」后，停止行走、重力下落、跳跃、攀爬和特殊行为位移；正在进行的分身与叠箱子会结束清理。仍可手动拖动，松手后停在新位置，也可摸头、喂饭、玩耍和睡觉。设置和位置会保存，取消勾选后恢复正常物理及探索规则。此开关与「自动探索」独立。

首次安装默认登记当前用户开机自启动，不要求管理员权限；更新时检查实际登记，不再仅依赖旧的安装标记。启动项路径失效会尝试修复。在软件菜单里主动关闭的选择会保存并在更新后保留。设备管理策略若禁止所有自启动登记，软件无法保证绕过系统限制。

诺艾尔采用银发、红玫瑰头饰与女仆骑士装的 Q 版形象，包含待机、行走、跳跃、睡眠、攀爬、悬挂、进食，以及现有的九种特殊行为。行走、攀爬、悬挂、擦屏幕、两种旗帜和影分身结印各有八帧；箱子继续使用共享木箱素材。切换皮肤不改变动作概率和冷却。

皮肤包已随程序附带，使用时不需要联网下载或重新生成图片。新增角色的动作图属于 AI 生成的同人素材，素材来源见 NOTICE.md。

## 自主行动与概率

启动后自主活动，不需要点击动作菜单。满足冷却条件、可以选新活动时：
- 移动／找边框攀爬：75%。包含走路和有位移的跳跃，根据可达距离、当前位置和最近走过的路线选择目的地。
- 特殊动作：25%。轻跳、叠箱子、敲侧边框、擦屏幕、原神横幅、WKCO 举旗、挥手、伸懒腰、影分身，共 9 种。每轮将九种随机排序，各出现一次后再洗牌，避免反复只看到少数动作；长期次数均等。

上述比例是符合条件时每次决策的抽取概率，不是各动作占用时间。特殊动作之间设置 45 秒冷却，启动前 30 秒不抽特殊动作。走向屏幕边框、攀爬、转角和跳跃期间不重新抽动作；到达顶部悬挂后至少活动 10 秒。需要位置条件时自行准备：敲边框先找侧边，地面动作先落地站稳。
敲边框、擦屏幕、举横幅、WKCO 举旗、挥手、伸懒腰每次持续 10～14 秒；轻跳连续 2～3 次。箱子批次和持续流程保持原规则。找屏幕侧边时随机选择左侧或右侧；悬挂横移分段随机换向，侧爬偶尔短暂下探 1.5～3 秒后继续上爬，转角保留向内行进方向。
移动跳跃带有明确水平位移；窗口边缘提供多个可达落点。同一窗口同一面的跳跃路线尝试后暂避 15 秒，敲边框准备超过 60 秒则改走其他活动，稍后再尝试。普通活动中若 4 秒内多次落回同一点，会主动跳离；箱子和连续小跳不触发这项保护。

## 影分身（v1.4.1）

影分身会自主抽取，也可以在右键“特殊动作”菜单中体验。本体先完成约 1.8 秒的结印；2～30 个分身从本体身后依次出现，生成后立即跳向随机可达的任务栏、窗口上沿/侧边/下沿或屏幕边缘，附着后继续行走、攀爬或悬挂。分身彼此不碰撞、不推挤，落点选择仍尽量分散。每个分身出生后自由活动 10 秒，再原地表演一次随机特殊动作，动作池不包含影分身；每轮至少两个分身举「温州肯恩大学室内乐团WKCO牛逼」旗。表演期间临时暂停该分身的运动与重力，不要求先站到地面，也不寻找其他位置。结束后恢复正常运动与接触判定，原窗口移动或消失时从原地松手下落。本体的限制不变。分身叠箱子使用临时表演道具，最多显示四个，动作结束后清理，不占用本体箱子。完整动作从结印开始计时 45 秒，最后 2.2 秒播放漫画烟雾消失，只保留本体。

数量不包含本体。数量 n 的抽取权重为 0.87^(n-2)，在 2～30 之间归一化；每增加一个分身，概率是前一个数量的 87%。2 个约 13.23%，30 个约 0.27%，平均约 8.17 个。分身共享当前皮肤与缩放，不会再生成分身，也不会重复启动硬件监测或更新任务。隐藏/退出桌宠或主动切换动作会提前收起分身，避免残留窗口。特效窗口不拦截鼠标。

## 箱子规则

选中叠箱子后，一批累计放 2～10 次；九种次数等概率，各 1/9。
同一时刻最多保留 4 个。第五个及以后每放一个新箱子，最旧的一个消失。
她会连续起跳、垫箱、落到箱顶；上方空间不足时自行移到较低位置继续。完成整段动作并落稳后，所有箱子清空，桌宠正常下落。
箱子边长为 140 × 桌宠缩放比例，比上一版缩小三分之一，无文字，使用生成的木箱图片。可站立、攀爬，且独立悬浮，下层箱子移除不会带动上层下落。
站立面位于箱子顶面的前沿；叠放时上层箱底覆盖下层顶面，缩放保持这层接触关系。
右键箱子可提前收起，左键可拖动；这些只是可选干预，自动流程不依赖点击。

## 形象与动画

攀爬、悬挂、擦拭、举横幅、WKCO 举旗各使用 8 张完整角色图片；敲击、挥手、伸懒腰各使用 4 张。侧爬保留原动作并按面部特征校准坐标，悬挂改为两轮交替换手和轻微屈膝，支撑手对齐同一边缘高度。擦拭始终用同一只手，沿连续轨迹往返。站立动作统一脚底位置，不再使用程序拼接的手臂。
各组帧采用统一缩放基准，背景已预先抠除为透明图片；不使用半透明混帧，避免身体淡出。移动和重力约 60 Hz，图片姿态为关键帧播放，并非每秒 60 张独立绘画。
横幅文字“原神牛逼”直接包含在生成的横幅帧中，随布面和角色一起晃动。
另有独立举旗动作，旗面分三行写“温州肯恩大学／室内乐团／WKCO牛逼”。文字直接包含在旗帜图片中；举旗时临时扩展显示空间，角色脚底坐标保持不变。

## 拖动、缩放与窗口互动

拖到窗口侧边或下沿附近松手会尝试抓挂，离边缘较远时下落。普通可见顶层窗口四边、自己的电脑状态窗以及箱子都可互动。
跳抓下沿按手部向上碰到边缘判定；窗口关闭、最小化、被遮挡或离开抓握位置后失去支撑。
滚轮缩放桌宠（30%～150%），默认 55%；箱子同步缩放，行走位移随大小变化。
右键菜单可暂停自动探索、查看可选动作或退出。箱子在退出时清空。
应用内部按钮、网页元素不在碰撞范围。不同缩放比例的多屏未全面验证。

## 电脑状态

CPU／GPU 占用与温度、物理内存、显存每 1 秒更新。电脑状态窗独立拖动、最小化和关闭；每次启动默认显示。
右键状态窗或桌宠 →“电脑状态透明度”，支持 0%～75%，立即生效并保存。
CPU 温度使用 LibreHardwareMonitor 与 PawnIO，部分设备还需要管理员采集进程。无效 0°C 和过期数据不显示为有效温度。GPU 优先使用 NVIDIA 驱动数据。

## 自启动与设置

已有当前用户计划任务 BlueWhalePet-用户名，登录后延迟 8 秒启动，允许电池启动，失败重试 3 次。右键可开关；移动程序文件夹后重新开关以更新路径。
设置和实时传感器数据位于 %LOCALAPPDATA%/BlueWhalePet，startup.log 记录启动结果。未重启 Windows 进行完整登录实测。
runtime.json 每 5 秒记录动作、位置、探索开关与最近特殊动作，供排查停住问题；不记录窗口内容。
卸载前关闭自启动，再退出并删除程序文件夹。角色署名及使用范围见 NOTICE.md。完整源码在 source，运行时动作图位于 assets/skins；生成原图留在制作工程，精简安装包不重复携带。


## 自动更新与安装

双击 Install.cmd 安装后直接开启登录启动，不再询问；直接运行 EXE 的首次启动也会初始化自启动。右键可手动关闭，之后自动更新保留选择。
启动约 8 秒后后台检查 GitHub 正式 Release，之后每 30 分钟重试；发现新版本显示版本号和更新说明。立即更新会显示 ZIP 下载进度，校验后重启安装；稍后提醒将同一版本延后 4 小时。网络失败不影响桌宠和电脑状态监测。
正式更新只认 Release，普通 git push 不触发。COS / OSS 备用源与签名配置详见《更新与发布说明.md》。


首次安装及更新启动后，自动在当前用户的实际桌面（包括 Windows 重定向的 OneDrive 桌面）创建或更新“WKCO桌宠”快捷方式，目标与工作目录指向当前安装位置。正常重启不反复创建；手动重新运行安装器或完成更新时会补建。快捷方式创建失败不会阻断桌宠启动。

## 静默更新（v1.4.0）

首次安装以及从未设置此选项的旧版升级，默认开启静默更新。启动约 8 秒后在后台检查正式 GitHub Release，此后每 30 分钟检查一次。发现新版本后自动下载、校验、准备并重启桌宠，全程不显示更新询问、下载进度、成功通知或失败弹窗。下载失败继续使用当前版本，稍后重试。下载仍有单次连接 30 秒、整个下载 1 小时的限制，仍验证更新包哈希和备用清单签名。普通 git push 不触发更新。

右键桌宠或托盘 → 检查更新 → 取消勾选「静默更新（后台下载并自动重启）」，恢复原来的新版本弹窗、立即更新和稍后提醒。关闭状态会保存，更新后继续尊重此选择；关闭时会取消正在进行的静默下载。静默开启时，「立即检查」也会静默执行。
