# 鲸鱼娘桌宠（BlueWhalePet）卸载与清理指南

适用版本：v1.4.4 后版本· 更新日期：2026-09-10

本指南说明如何**关闭开机自启动**，以及如何**彻底删除**桌宠及其配套的文件保护组件。

---

## 一、先理解为什么不能直接删文件夹

桌宠实际上包含**两个互相独立的组件**：

| 组件 | 位置 | 它做什么 |
|---|---|---|
| 桌宠本体 `BlueWhalePet.exe` | `%LOCALAPPDATA%\Programs\BlueWhalePet` | 桌面角色、监控面板、自动更新 |
| 文件保护 `WKCOProtect.exe` | `%LOCALAPPDATA%\Programs\protecting` | 登录后检查程序文件完整性，损坏或缺失时从正式 Release 静默重新下载修复 |

两者之间有一个**互相补登记**的关系，这就是「删了又回来」的原因：

- 保护程序登记在「当前用户登录启动」，登录后约 45 秒检查一次。**如果发现程序文件缺失，它会从 Release 把整套程序重新下载并还原。**
- 桌宠本体每次启动时，也会重新确保「自己的自启动登记」和「保护程序的启动项」都存在。

所以只删文件夹、或只删其中一个启动项，都会被另一个补回来。**必须按顺序做。**

---

## 二、只想关掉开机自启动？做第 1、2 步就够了

如果你还想留着桌宠、只是不想开机自动启动：执行下面的**第 1 步**和**第 2 步**即可，不要删目录。

---

## 第 1 步：先关闭文件保护（必须最先做）

1. 打开文件夹：`%LOCALAPPDATA%\Programs\protecting`
   （可直接在文件资源管理器地址栏粘贴这行，或展开为 `C:\Users\<你的用户名>\AppData\Local\Programs\protecting`）
2. **双击 `prohibit.vbs`**
   - 注意：它是 **VBScript 脚本文件**，不是文本说明。如果双击后弹出「打开方式」选择框，说明你的文件关联被改了，改用：右键 → 打开方式 → Microsoft Windows Based Script Host；或在该文件夹地址栏输入 `wscript prohibit.vbs` 回车。
3. **没有任何提示是正常的**。它静默执行，不会弹窗、不会报告成功。

它做了什么：

- 在同目录生成 `disabled` 标记文件（内容为 `Disabled by the user. Updates must preserve this choice.`）
- 删除注册表启动项 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\WKCODesktopProtection`

**验证是否成功：**

- [ ] `protecting` 文件夹里出现了 `disabled` 文件（没有扩展名）
- [ ] 打开「任务管理器 → 启动应用」，列表里不再有 `WKCODesktopProtection`
- [ ] 命令行核对：`reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Run"` 输出里没有 `WKCODesktopProtection`

> **为什么必须用 `prohibit.vbs` 而不是手动删注册表？**
> 手动删注册表只能撑到下一次更新或重装——保护程序会重新登记。而 `disabled` 标记是**持久的**：它的注释明确写了「更新必须保留这个选择」，之后的重装和更新都会被尊重。
> 除非你手动删掉 `disabled` 文件，否则它不会自己恢复。

---

## 第 2 步：关闭桌宠自己的开机自启动

桌宠的自启动是**当前用户的登录计划任务**，名字是 `BlueWhalePet-<你的用户名>`（例如 `BlueWhalePet-联想`）。

**方法 A（推荐，最省事）**
托盘图标右键 → 取消勾选「开机自启动」。
它会把你的选择写入 `%LOCALAPPDATA%\BlueWhalePet\startup-preferences.json`，之后正常启动和自动更新都会保留这个选择。

**方法 B（手动删除计划任务）**
按 `Win + R`，输入 `taskschd.msc` 打开任务计划程序 → 左侧点「任务计划程序库」→ 在中间列表找到 `BlueWhalePet-<用户名>` → 右键删除。

或用管理员命令行：

```cmd
schtasks /delete /tn "BlueWhalePet-%USERNAME%" /f
```

**验证是否成功：**

- [ ] 任务计划程序库根目录下没有 `BlueWhalePet-*` 任务
- [ ] 命令行 `schtasks /query /tn "BlueWhalePet-%USERNAME%"` 返回「找不到」

> **注意**：第 2 步只管桌宠。如果你跳过第 1 步，保护程序下次登录**仍然会运行**（但它不会再主动拉起桌宠，只会检查文件完整性）。

---

## 第 3 步：退出桌宠进程

**必须先退出，否则文件被占用删不掉。**

托盘图标右键 → **退出**。

> **常见误区**：点窗口右上角的 `×` **不是退出**，只是把角色隐藏起来，进程仍在运行。

如果托盘里找不到图标，或退出失败，直接结束进程（在有管理员权限的 PowerShell 里执行）：

```powershell
Get-Process BlueWhalePet, SensorHost, WKCOProtect -ErrorAction SilentlyContinue | Stop-Process -Force
```

`SensorHost.exe` 是 CPU 温度读取组件，以管理员权限运行，普通权限可能杀不掉；任务管理器里如仍存在，以管理员身份结束即可。

---

## 第 4 步：删除文件与启动项

### 4.1 删除目录

| 要删的目录 | 内容 | 本机实测体积 |
|---|---|---|
| `%LOCALAPPDATA%\Programs\BlueWhalePet` | 桌宠程序本体 | 约 137 MB |
| `%LOCALAPPDATA%\BlueWhalePet` | 设置、日志、更新缓存 | 约 611 MB |
| `%LOCALAPPDATA%\Programs\protecting` | 保护组件（含 `disabled` 标记） | 约 29 MB |
| `%LOCALAPPDATA%\Programs\.BlueWhalePet-*` | 安装/修复过程的残留备份与暂存 | 约 1,125 MB（本机现有 21 个） |

> **删 `%LOCALAPPDATA%\BlueWhalePet` 会丢失**：角色位置与缩放、选择的皮肤、监控面板设置、自定义皮肤引用（自定义皮肤本体在程序目录的 `assets\skins\` 下，需要单独备份）。

### 4.2 删除快捷方式

桌面上：`WKCO桌宠.lnk`
旧版本可能残留：`鲸鱼娘与诺艾尔桌宠.lnk`

本程序**不创建开始菜单项**，通常无需处理；如果你自己建过请一并删除。

### 4.3 清理可能残留的注册表项

```cmd
reg delete "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v WKCODesktopProtection /f
reg delete "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v BlueWhalePet /f
```

---

## 第 5 步（可选）：移除 CPU 温度驱动 PawnIO

**只有在你曾经点过「CPU 温度」并完成了随附驱动安装时才需要处理。**

检查是否装了：

```powershell
Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\PawnIO'
```

返回 `True` 表示已安装。它是**按需启动**（不会开机自启），属于第三方通用硬件访问驱动，LibreHardwareMonitor 等工具也会使用。

**如果其他监控软件还在用它，建议保留。** 确认不再需要时，以管理员身份执行：

```powershell
pnputil /enum-drivers | findstr /i pawnio
pnputil /delete-driver pawnio.inf /uninstall /force
```

---

## 三、验证清单（重启后再检查一遍）

- [ ] 任务计划程序里没有 `BlueWhalePet-*`
- [ ] `reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Run"` 里没有 `WKCODesktopProtection` 和 `BlueWhalePet`
- [ ] `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup` 文件夹为空（本程序不往这里写，确认没被写入）
- [ ] 上面三个目录都不存在
- [ ] 任务管理器里没有 `BlueWhalePet`、`SensorHost`、`WKCOProtect` 进程
- [ ] 重启后等待 1 分钟，程序没有自己回来

---

## 四、一键脚本（可选）

### A. 只关闭开机自启动，保留程序与设置

```powershell
# 关闭文件保护（写入持久 disabled 标记）
wscript.exe "$env:LOCALAPPDATA\Programs\protecting\prohibit.vbs"

# 删除桌宠登录任务
schtasks /delete /tn "BlueWhalePet-$env:USERNAME" /f

# 核对结果
reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Run"
schtasks /query /tn "BlueWhalePet-$env:USERNAME"
```

### B. 彻底卸载（会删除全部设置与皮肤，不可恢复）

```powershell
# 1) 先关保护，防止它把程序重新装回来
wscript.exe "$env:LOCALAPPDATA\Programs\protecting\prohibit.vbs"
Start-Sleep -Seconds 2

# 2) 删除自启动任务
schtasks /delete /tn "BlueWhalePet-$env:USERNAME" /f 2>$null

# 3) 结束进程
Get-Process BlueWhalePet, SensorHost, WKCOProtect -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 2

# 4) 删除目录（含安装残留）
Remove-Item "$env:LOCALAPPDATA\Programs\BlueWhalePet" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "$env:LOCALAPPDATA\BlueWhalePet" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "$env:LOCALAPPDATA\Programs\protecting" -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem "$env:LOCALAPPDATA\Programs" -Filter '.BlueWhalePet-*' -Force -Directory -ErrorAction SilentlyContinue |
    Remove-Item -Recurse -Force -ErrorAction SilentlyContinue

# 5) 删除快捷方式
$desktop = [Environment]::GetFolderPath('Desktop')
Remove-Item "$desktop\WKCO桌宠.lnk" -Force -ErrorAction SilentlyContinue
Remove-Item "$desktop\鲸鱼娘与诺艾尔桌宠.lnk" -Force -ErrorAction SilentlyContinue

# 6) 清理注册表启动项
Remove-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run' -Name WKCODesktopProtection -ErrorAction SilentlyContinue
Remove-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run' -Name BlueWhalePet -ErrorAction SilentlyContinue

Write-Host '卸载完成。建议重启后按第三节的清单核对一遍。'
```

---

## 五、常见问题

**Q：删掉文件夹之后，下次开机它又回来了。**
第 1 步没做。保护程序在登录时发现文件缺失，从 Release 重新下载还原了整套程序。请按 1 → 2 → 3 → 4 的顺序重新做一遍。

**Q：双击 `prohibit.vbs` 完全没反应，是不是没生效？**
正常。它设计上就是静默执行，不显示任何窗口或提示。请按第 1 步的验证清单确认 `disabled` 文件是否生成。

**Q：关掉自启动之后，自动更新会不会又把它打开？**
不会。你的选择保存在 `startup-preferences.json`，更新流程会保留它。**但**如果你重新运行一次 `Install.cmd` 安装器，会按安装默认值重新开启自启动——因为安装等同于一次全新安装。

**Q：控制面板 / 设置 的「已安装的应用」里找不到卸载入口。**
正确，本程序目前不注册系统卸载项，需要按本指南手动清理。

**Q：我只是不想让它开机启动，但想留着这个桌宠。**
只做第 1、2 步。

**Q：电脑上有多个 Windows 账户。**
每个账户各自有一份 `%LOCALAPPDATA%`、各自的 Run 项和登录任务。**需要在每个用过的账户里分别处理**，否则换个账户登录它照样会启动。

**Q：公司/学校的设备管理策略禁止了所有自启动登记。**
本程序无法绕过系统策略。这种情况下它本来也不会自动启动。

---

## 附：本指南涉及的全部路径

```
%LOCALAPPDATA%\Programs\BlueWhalePet\      程序本体（含 source、assets\skins、sensors）
%LOCALAPPDATA%\BlueWhalePet\               设置与缓存（settings.json、startup.log、updates\）
%LOCALAPPDATA%\Programs\protecting\        保护组件（prohibit.vbs、disabled、protection.log）
%LOCALAPPDATA%\Programs\.BlueWhalePet-*\   安装/修复残留（可安全删除）
桌面\WKCO桌宠.lnk                          桌面快捷方式

HKCU\Software\Microsoft\Windows\CurrentVersion\Run\WKCODesktopProtection
任务计划程序库\BlueWhalePet-<用户名>

HKLM\SYSTEM\CurrentControlSet\Services\PawnIO    （可选，CPU 温度驱动）
```

更多信息见程序目录下的 `README.md`、`使用说明.md` 与 `NOTICE.md`。
